# 이룸 LMS — 작업 기록 (project.md)

> 세션마다 결정 사항 · 구조 · 남은 일을 여기에 덧붙입니다.
> 새 세션을 시작하면 이 파일을 먼저 읽고 한 문장으로 요약한 뒤 이어서 진행합니다.

---

## 2026-08-19 — 이룸토익 학원 추가 (v7.0.0)

### 무엇을 했나
기존 이룸편입 LMS 안에 **이룸토익 학원**을 추가했습니다. 하나의 앱·하나의 서버·하나의 계정 체계로
두 학원을 함께 운영합니다.

### 결정 사항 (Kevin 확인)
| 항목 | 결정 |
|------|------|
| 통합 구조 | **같은 앱 + 학원 전환** (별도 앱 복제 아님) |
| 1차 범위 | 핵심부터 — 문제은행 · 모의고사 · 성적/분석 · 학생/반 관리 |
| LC 음원 | 업로드 · 재생 포함 (실전 모드 1회 제한) |
| 편입 「지원가능 대학」 대체 | 목표 점수 달성 예측 + 시험 일정 + 파트별 약점 플랜 + 기업 커트라인 매칭 (4개 모두) |

### 학원 전환이 동작하는 방식 (app_ac.js)
- 모든 레코드에 `ac` 필드를 둡니다. `'transfer'`(편입) / `'toeic'`(토익).
- **`ac` 가 없으면 항상 편입으로 봅니다.** 그래서 기존 데이터를 손대지 않아도 그대로 동작합니다.
- 화면에서 목록을 읽을 때는 `acf(DB.students)` 처럼 감싸 현재 학원 것만 걸러 냅니다.
  `acf()` 는 같은 객체를 담은 새 배열이라 `find()` 로 찾아 값을 고쳐도 원본이 바뀝니다.
- **삭제/추가(`DB.X = ...`, `DB.X.push(...)`)는 감싸지 않았습니다.** 감싸면 다른 학원 데이터가
  통째로 지워집니다. 앞으로 코드를 고칠 때도 이 원칙을 지켜 주세요.
- 새로 만든 레코드는 `save()` 안의 `acStampNew()` 가 자동으로 현재 학원으로 도장을 찍습니다.
  서버에서 받아온 레코드는 `resetSnap()` → `acSeenReset()` 으로 기준이 다시 잡히므로 도장이 찍히지 않습니다.
- 관리자만 사이드바에서 학원을 전환할 수 있습니다(`acCanSwitch`). 학생·강사는 자기 학원으로 고정됩니다.
  관리자 레코드에 `acLock:true` 와 `ac` 를 넣으면 그 학원 전용 관리자가 됩니다.
- 기본 데모 계정(a1/i1/s1/s2/demo)은 `ac:'transfer'` 로 고정해 두었습니다.

### 새로 만든 파일
| 파일 | 역할 |
|------|------|
| `app_ac.js` | 학원 전환 공통 뼈대 (acf / acStamp / acSet / acPaint) |
| `app_toeic_core.js` | Part 1~7 정의, 환산점수표, 반 편성, 목표 예측, 시험 일정, 커트라인 |
| `app_toeic_bank.js` | 문제은행 시드(124문항 · 세트 14개), 출제 로직, 모의고사 구성 |
| `app_toeic_exam.js` | 시험 엔진 (LC 음원 · 지문 세트 · 채점 · 환산점수 · 다시보기) |
| `app_toeic_views.js` | 학생 화면 9종 |
| `app_toeic_admin.js` | 관리자 · 강사 화면 7종 |

### 고친 파일
- `app_core.js` — `save()` 에 `acStampNew()`, `resetSnap()` 에 `acSeenReset()`, SHAPE 키에 토익 저장소 추가, 시드 데이터에 `ac:'transfer'`
- `app_boot.js` — 로그인 화면 학원 선택, 로그인 시 소속 학원 자동 전환, `navOf()` 로 해시 라우팅, `Net.busy()` 에 토익 시험 오버레이 추가
- `app_views.js` — `NAV_TOEIC` / `navOf()`, 사이드바 학원 브랜딩·전환 버튼, `renderPage` 에 토익 화면 합치기
- `server.js` — `ARR_KEYS`/`OBJ_KEYS` 에 토익 저장소, 체험 계정 스코프에 토익 개인기록
- `styles.css` — 학원 테마 변수(`--side1/2`), `.ac-*`, `.tex-*` 스타일
- 그 외 15개 모듈 — 목록 읽기를 `acf(...)` 로 감쌈 (자동 패치 후 전수 확인)
- `build.py` — **없어진 `data.js` 를 읽으려다 실패하던 문제를 고쳤습니다.**
  이제 데이터 블록(QUESTIONS/UNIVERSITIES/WORDS/IDIOMS/UNI_*)은 그대로 두고 CSS·모듈만 교체합니다.
  조립 전에 모든 모듈을 `node --check` 로 검사하고, 하나라도 깨지면 멈춥니다.

### 새 저장소 키
배열: `toeicQ` `toeicSets` `toeicSessions` `toeicExams` `toeicDates` `toeicCuts`
객체: `toeicConf` `toeicGoal` `toeicWrong` `toeicWord`

### 환산 점수에 대한 주의 (중요)
ETS 는 회차별 환산표를 공개하지 않습니다. `TO_SCALE_LC` / `TO_SCALE_RC` 는 공개된 기준점
(LC 27개=60 · 33개=110 · 56개=275 · 75개=390 · 89개=470 · 93개↑=495 /
 RC 39개=110 · 63개=275 · 88개=390 · 95개=470 · 100개=495)을 직선 보간해 5점 단위로 만든 **예상 표**입니다.
화면에도 «예상»이라고 표시했고, `DB.toeicConf.scaleLC / scaleRC` 에 101칸 배열을 넣으면
학원 기준표로 바꿀 수 있습니다. (아직 그 편집 화면은 만들지 않았습니다 — 아래 남은 일 참고)

### 시험 일정 데이터
2026년 정기시험은 제560회~제585회, 26회입니다. 공식 사이트를 직접 읽을 수 없어
**시험일·성적발표일은 확인된 값**, **회차 번호 일부는 추정**으로 넣고 그 사실을 표에 표시했습니다.
제576회(2026-08-23, 접수 07-06~08-10, 발표 09-03)만 접수 기간까지 확인했습니다.
관리자 화면에서 회차별 수정 · 일괄 붙여넣기로 정정할 수 있습니다.

### 커트라인 데이터
기업·기관이 공식적으로 밝히지 않는 경우가 많아 **학원이 관리하는 참고 값**으로 넣었습니다.
학생 화면에도 그렇게 명시했습니다. 실제 채용 공고 기준으로 관리자가 고쳐 쓰면 됩니다.

### 검증 (실제 브라우저)
Chromium 헤드리스로 클릭까지 확인했습니다.
- 토익: 로그인 학원 선택 → 관리자 전환 → 관리자 화면 7종 → 문항 추가 → 학생 화면 9종
  → 파트 연습 응시·채점·해설 → 하프 모의고사 74문항 응시 → 예상 점수 산출 → 점수/오답/예측 반영 : **51/51 통과**
- 편입 회귀: 학생 17화면 · 강사 13화면 · 체험 2화면 : **32/32 통과**
- 문제은행 데이터 검증: 보기 수 · 정답 인덱스 · 해설 · 세트 연결 · 유형 일치 · id 중복 : 이상 없음
- 콘솔 오류는 Google Fonts 차단(샌드박스 네트워크) 뿐 — 실서비스에서는 나지 않습니다.

### 남은 일 (다음 세션)
1. **토익 빈출 단어** 화면 — 편입의 「데일리 단어」에 해당하는 토익판. 어휘 데이터 필요.
2. **환산표 편집 화면** — `DB.toeicConf.scaleLC/scaleRC` 를 관리자가 표로 고칠 수 있게.
3. **Part 1 사진 · LC 음원 실제 등록** — 업로드 기능은 만들었으나 실제 콘텐츠는 학원이 넣어야 합니다.
   문제은행 시드 124문항은 예시이며, 실전 200문항 모의고사를 하려면 파트별로 더 필요합니다
   (Part 2 25 · Part 3 39 · Part 4 30 · Part 6 16 · Part 7 54).
4. **LC 자동 진행 모드** — 지금은 실전 모드에서 세트 진입 시 1회 자동 재생. 실제 시험처럼
   전체 45분 흐름을 강제하는 모드는 아직 없습니다.
5. **알림톡 · 달력 · 과제**를 토익 문구로 다듬기 (현재는 편입 화면을 그대로 씁니다).
6. 토익 학생 레벨테스트(반 배정용 축약 시험) — 지금은 모의고사 최고 점수로 반을 정합니다.

### 배포
```
python3 build.py          # public/index.html 재조립 (모듈 문법 검사 포함)
git add -A && git commit -m "이룸토익 학원 추가"
# 서버에서
cd /root/eroom-app && git pull
pm2 delete eroom 2>/dev/null; pm2 start ecosystem.config.js; pm2 save
```

---

## 2026-08-19 (2차) — 토익 빈출 어휘 (v7.1.0)

### 무엇을 했나
「남은 일 1번」이던 토익 빈출 어휘 화면을 만들었습니다. 학생용 `ts-word`, 관리자용 `ta-word`.

### 자료를 어떻게 모았나 (웹 · 유튜브 리서치)
- **주제 분류는 ETS 공식 문서에서 가져왔습니다.** TOEIC Listening and Reading Test Examinee Handbook 에
  적힌 출제 영역 13개(Corporate Development / Dining Out / Entertainment / Finance and Budgeting /
  General Business / Health / Housing·Corporate Property / Manufacturing / Offices / Personnel /
  Purchasing / Technical Areas / Travel)를 그대로 주제 키로 썼습니다. 가장 신뢰도가 높은 근거입니다.
- 국내외 어휘 정리 사이트 3곳(engquiz.pro / lingomelo / estudyme)의 분류를 교차 확인했고,
  모두 ETS 13개 영역과 사실상 일치했습니다 → 분류가 맞다는 확인.
- 국내 강의·블로그 자료에서 **무엇을 함께 가르치는지**를 확인했습니다:
  Part 5 어휘는 **콜로케이션(짝 표현)** 중심, Part 7 은 **패러프레이징(동의어 치환)** 중심,
  그리고 **혼동어휘**(personal/personnel 류). 그래서 단어마다 col · syn · conf 세 칸을 넣었습니다.
- 유튜브: 해커스토익 「토익 직전 시험에 출제된 단어」 재생목록(68편) 등을 확인했지만,
  **영상 페이지가 자바스크립트로 그려져 목록·자막을 읽을 수 없었습니다.** 그래서 영상에서
  단어를 그대로 가져오지는 못했고, 위 자료로 확인한 «무엇을 가르치는가»만 반영했습니다.
- **단어와 예문은 이룸토익이 직접 정리했습니다.** 시중 단어장을 그대로 옮기면 저작권 문제가 있어,
  ETS 영역 분류에 맞춰 새로 작성했습니다. 화면에도 그 사실을 적어 두었습니다.

### 자료 규모
- 단어 **227개** — 핵심 어휘 190 + Part 7 패러프레이징 짝 20 + 혼동어휘 짝 12 (일부 중복 정리)
- 난이도 기초 53 / 중급 87 / 고급 87
- 주제별: 사무 35, 일반 비즈니스 35, 재무 26, 인사 21, 출장 16, 구매 16, 제조 14,
  기술 13, 주택 13, 문화 10, 보건 10, 기업개발 9, 식사 9

### 새 파일
| 파일 | 역할 |
|------|------|
| `app_toeic_voca.js` | 어휘 데이터 227개 + 주제 정의(TO_THEMES) + 접근 함수 |
| `app_toeic_word.js` | Day 덱 구성 · 암기 기록 · 복습 순위 · 단어 테스트 엔진 · 학생/관리자 화면 |

### 고친 파일
- `app_views.js` — NAV 에 `ts-word`(학생 학습 그룹) · `ta-word`(관리자·강사 토익 그룹), 라우터 등록
- `app_toeic_core.js` — `toeicInit()` 에 `DB.toeicVoca` 초기화
- `app_toeic_views.js` — 홈 「오늘 할 일」에 어휘 Day·진도 연결
- `app_core.js` · `server.js` — 저장소 키 `toeicVoca` 추가
- `styles.css` — `.tw-*`(단어 카드), `.twq-*`(단어 테스트)
- `build.py` — 모듈 순서에 두 파일 추가

### 데이터 구조
```
DB.toeicVoca                 학원이 추가한 단어 (배열)
DB.toeicConf.wordPerDay      하루 분량 (5~100, 기본 30)
DB.toeicWord[studentId] = {
  st:   { 단어id: 1(외움) | 2(헷갈림) },
  seen: { 단어id: 마지막으로 본 날 },
  log:  { 날짜: 그날 새로 확인한 개수 },     ← 연속 학습일 계산에 씀
  day:  현재 Day,
  tests:[ {date, mode, total, right, wrong:[단어id]} ]
}
```

### 함께 고친 버그 두 가지 (기존 코드의 문제)
1. **로그아웃 직후 저장 유실** — `Net.push` 는 0.5초 뒤에 보내도록 예약됩니다. 그 전에 로그아웃하면
   `location.reload()` 가 요청을 취소해 방금 만든 자료가 사라졌습니다.
   → `Net.flush()` 를 추가하고, `logout()` 이 flush 응답을 기다린 뒤에 새로고침합니다(최대 2초).
   창을 닫을 때(`pagehide`/`beforeunload`)도 flush 를 시도하며, 자료가 60KB 미만일 때만
   `sendBeacon` 을 씁니다(beacon·keepalive 는 64KB 제한).
   서버는 beacon 을 위해 `POST /api/state?beacon=1&t=토큰` 을 PUT 과 같이 처리합니다
   (쿼리 토큰은 beacon 요청에만 인정합니다).
2. **빈 목록이 서버 자료를 지우던 경로** — `deepMerge` 는 빈 배열이 오면 서버 배열을 그대로 교체했습니다.
   동기화가 늦은 기기 하나가 학생·강의·평가를 통째로 비울 수 있었습니다.
   → 알려진 목록 키(ARR_KEYS)에 대해 **빈 배열은 「바뀐 것 없음」으로 무시**합니다.
   삭제는 기존처럼 `_deletedIds` 툼스톤으로만 전파됩니다.
3. 모달을 배경 클릭으로 닫아도 화면이 갱신되도록 `onModalClose()` 훅을 추가했습니다.

### 검증
- 어휘 스모크 34개 항목 전부 통과 (필터·검색·추가·일괄등록·하루분량·Day 이동·뜻 가리기·
  암기 표시·복습 목록·테스트 3방식·채점 값 일치·기록 저장·홈 연결·진도 숫자 일치)
- 단어 227개 전수 검사 — 항목 누락·품사·주제·난이도·파트·id 중복·단어 중복·예문 포함 여부 : 이상 0건
- 회귀 — 토익 51개, 편입 32개 화면 전부 통과

### 남은 일 (다음 세션)
1. **환산표 편집 화면** — `DB.toeicConf.scaleLC/scaleRC` 를 관리자가 표로 고치는 UI
2. **어휘 확장** — 227개는 시작점입니다. 500~1000개로 늘리려면 일괄 등록 창에 붙여 넣으면 됩니다
   (형식은 관리자 화면에 안내되어 있습니다)
3. **Part 1 사진 · LC 음원** 실제 콘텐츠 등록 (기능은 완성, 파일은 학원이 넣어야 함)
4. **LC 45분 자동 진행 모드**
5. 알림톡 · 달력 · 과제 문구를 토익용으로 다듬기
6. 토익 레벨테스트(반 배정용 축약 시험)
7. 단어 발음 듣기(TTS) — 지금은 텍스트만 제공

---

## 2026-08-19 (3차) — 환산표 편집 화면 · 어휘 618개 (v7.2.0)

### 무엇을 했나
남은 일 1번(환산표 편집 화면)과 2번(어휘 확장)을 함께 처리했습니다.

### 환산표 리서치 결과
- **ETS는 공식 환산표를 공개하지 않습니다.** 출처마다 값이 꽤 다릅니다.
  예를 들어 RC 38개는 어떤 자료는 105점, 다른 자료는 185점으로 잡습니다.
- 그래서 «정답 하나»를 고르는 대신 **프리셋 3종 + 완전 편집**으로 만들었습니다.
  | 프리셋 | 내용 | 출처 |
  |--------|------|------|
  | 이룸 기본 | 공개 기준점 사이를 직선 보간 | global-exam 공개 기준점 |
  | 상세 추정표 | 개수마다 값이 다른 101칸 표 | 990prep 공개 자료 (오차 5~20점이라 밝힘) |
  | 선형(5점) | 5×개수 − 5 | Oxford English Testing 연습문제 환산 구간과 일치 |
- 화면과 결과 모두에 «예상»이라고 계속 표시합니다.

### 환산표 화면 (`ta-scale`)
- 프리셋 불러오기 / 기준점 보간(`27=60, 56=275…`) / 101개 붙여넣기 / 칸 직접 편집
- 검증: 101칸 · 5~495 · 5점 단위 · 단조 증가 → 위반 칸 빨강 표시 + 저장 잠금 + 자동 보정
- 만점 합계(LC+RC=990) 상시 확인, 저장 표(점선) vs 편집 표(실선) 그래프, 대표 지점 10곳 비교
- 저장은 `DB.toeicConf.scaleLC / scaleRC` (101칸 배열), 되돌리기는 **null 을 넣습니다**

### 어휘 확장 (227 → 618)
- 표제어 선정은 공개된 토익 어휘 목록을 교차 확인해 골랐습니다
  (engquiz.pro 주제별 50개 × 8 = 400개, lingomelo, estudyme, 비즈니스 콜로케이션 목록).
  **뜻·예문·짝표현·동의어·혼동어휘는 전부 직접 작성했습니다** — 시중 단어장을 옮기지 않았습니다.
- 새 파일 `app_toeic_voca2.js` 에 391개를 담고, `toVocaSeeds()` 가 모든 묶음을 합칩니다.
  앞으로 더 늘릴 때는 같은 파일에 `TO_VOCA_SEED9` 를 추가하고 `toVocaSeeds()` 에 한 줄만 더하면 됩니다.
- Part 5·6 을 겨냥해 형용사 38 · 부사 26 · 동사 53 · 전치사·접속사구 18 을 따로 넣었습니다.

### 세 번째 동기화 버그 (중요)
`deepMerge` 는 **들어오지 않은 항목을 「바뀐 것 없음」으로 봅니다.** 그래서
`delete DB.toeicConf.scaleLC` 처럼 값을 지우면 서버에 남아 있던 값이 그대로 되살아납니다.
환산표 되돌리기가 먹히지 않아 발견했습니다.
→ 지우는 대신 **`null` 을 명시해 넣었습니다.** 앞으로 「설정을 비우는」 기능을 만들 때는
반드시 같은 방식(빈 값을 명시)으로 하세요. 배열도 같은 이유로 빈 배열이 무시됩니다(2차 수정 참고).

### 검증
- 환산표 스모크 31개 항목 전부 통과
- 어휘 618개 전수 검사 이상 0건 (주제별 합계 = 전체 수 일치까지 확인)
- 회귀 — 어휘 34개 · 토익 51개 · 편입 32개 화면 모두 통과

### 남은 일 (다음 세션)
1. **Part 1 사진 · LC 음원** 실제 콘텐츠 등록 (기능 완성, 파일은 학원이 넣어야 함)
2. **문제은행 확장** — 지금 124문항. 실전 200문항 모의고사를 제대로 하려면
   Part 2 25 · Part 3 39 · Part 4 30 · Part 6 16 · Part 7 54 만큼 더 필요합니다
3. **LC 45분 자동 진행 모드**
4. 단어 발음 듣기(TTS)
5. 알림톡 · 달력 · 과제 문구를 토익용으로 다듬기
6. 토익 레벨테스트(반 배정용 축약 시험)
7. 어휘 1000개까지 확장 — 일괄 등록 창에 붙여 넣으면 됩니다
